## What

<!-- One or two sentences: what does this change do? -->

## Why

<!-- The problem it solves or the behavior it fixes. Link issues. -->

## Checklist

- [ ] `make test` passes (with `DATABASE_URL` set against a local Postgres if store/api code changed)
- [ ] `make lint` clean
- [ ] Docs updated if behavior changed (README/MANUAL/API/plugin README)
